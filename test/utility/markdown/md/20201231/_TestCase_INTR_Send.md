|用例编号|用例标题|预置条件<br>输入内容<br>预期结果|
|----------------|----------------|----------------|
|INTR_Send_000010|同时发起多个底层币发送交易_余额足够（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个底层币发送交易，每个交易的交易额都小于发起钱包的余额，总交易额也小于发起钱包的余额`<br><br>**预期结果**`交易都成功`|
|INTR_Send_000020|同时发起多个底层币发送交易_余额足够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000030|同时发起多个底层币发送交易_余额不够（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个底层币发送交易，每个交易的交易额都小于发起钱包的余额，但是总交易额大于发起钱包的余额`<br><br>**预期结果**`部分交易可能成功，部分交易可能失败，不会出现双花现象，不会出现其他异常情况`|
|INTR_Send_000040|同时发起多个底层币发送交易_余额不够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000031|同时向不同节点发起多个底层币发送交易_余额不够（通过FCJT_sendTransaction测试）|**预置条件**`同时发起多个底层币发送交易_余额不够（通过FCJT_sendTransaction测试）`<br><br>**输入内容**`在生成区块的间隔，某钱包同时向不同的节点发起多个底层币发送交易，每个交易的交易额都小于发起钱包的余额，但是总交易额大于发起钱包的余额`<br><br>**预期结果**`部分交易可能成功，部分交易可能失败，不会出现双花现象，不会出现其他异常情况`|
|INTR_Send_000041|同时向不同节点发起多个底层币发送交易_余额不够（通过FCJT_signTransaction测试）|**预置条件**`同时发起多个底层币发送交易_余额不够（通过FCJT_signTransaction测试）`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000050|同时发起多个代币发送交易_余额足够（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的代币`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个代币发送交易，每个交易的交易额都小于发起钱包的余额，总交易额也小于发起钱包的余额`<br><br>**预期结果**`交易都成功`|
|INTR_Send_000060|同时发起多个代币发送交易_余额足够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000070|同时发起多个代币发送交易_余额不够（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的代币`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个代币发送交易，每个交易的交易额都小于发起钱包的余额，但是总交易额大于发起钱包的余额`<br><br>**预期结果**`部分交易可能成功，部分交易可能失败，不会出现双花现象，不会出现其他异常情况`|
|INTR_Send_000080|同时发起多个代币发送交易_余额不够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000071|同时向不同节点发起多个代币发送交易_余额不够（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的代币`<br><br>**输入内容**`在生成区块的间隔，某钱包同时向不同节点发起多个代币发送交易，每个交易的交易额都小于发起钱包的余额，但是总交易额大于发起钱包的余额`<br><br>**预期结果**`部分交易可能成功，部分交易可能失败，不会出现双花现象，不会出现其他异常情况`|
|INTR_Send_000081|同时向不同节点发起多个代币发送交易_余额不够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000090|同时发起多个底层币、代币发送交易_余额足够（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币、代币`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个底层币、代币发送交易，每个交易的底层币、代币交易额都小于发起钱包的余额，总交易额也小于发起钱包的底层币、代币余额`<br><br>**预期结果**`交易都成功`|
|INTR_Send_000091|同时发起多个底层币、代币发送交易_余额足够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000100|同时发起多个底层币、代币发送交易_余额不够（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币、代币`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个底层币、代币发送交易，每个交易的底层币、代币交易额都小于发起钱包的余额，但是总交易额都大于发起钱包的底层币、代币余额`<br><br>**预期结果**`部分交易可能成功，部分交易可能失败，不会出现双花现象，不会出现其他异常情况`|
|INTR_Send_000101|同时发起多个底层币、代币发送交易_余额不够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000102|同时向不同节点发起多个底层币、代币发送交易_余额不够（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币、代币`<br><br>**输入内容**`在生成区块的间隔，某钱包同时向不同节点发起多个底层币、代币发送交易，每个交易的底层币、代币交易额都小于发起钱包的余额，但是总交易额都大于发起钱包的底层币、代币余额`<br><br>**预期结果**`部分交易可能成功，部分交易可能失败，不会出现双花现象，不会出现其他异常情况`|
|INTR_Send_000103|同时向不同节点发起多个底层币、代币发送交易_余额不够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000110|同时发送、接收底层币测试（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币`<br><br>**输入内容**`在生成区块的间隔，钱包A向另外一个钱包发送底层币（交易额大于余额），同时钱包B向钱包A发送底层币（可使钱包A的余额大于前面的交易额）；上述2个操作可以试着调整先后顺序看结果是否一致`<br><br>**预期结果**`钱包B向钱包A发送底层币的交易成功，钱包A的交易可能成功也可能失败，不会出现其他异常情况`|
|INTR_Send_000120|同时发送、接收底层币测试（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000111|同时向不同节点发送、接收底层币测试（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币`<br><br>**输入内容**`在生成区块的间隔，钱包A通过节点X向另外一个钱包发送底层币（交易额大于余额），同时钱包B通过节点Y向钱包A发送底层币（可使钱包A的余额大于前面的交易额）；上述2个操作可以试着调整先后顺序看结果是否一致`<br><br>**预期结果**`钱包B向钱包A发送底层币的交易成功，钱包A的交易可能成功也可能失败，不会出现其他异常情况`|
|INTR_Send_000121|同时向不同节点发送、接收底层币测试（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000130|同时发送、接收代币测试（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的代币`<br><br>**输入内容**`在生成区块的间隔，钱包A向另外一个钱包发送代币（交易额大于余额），同时钱包B向钱包A发送代币（可使钱包A的余额大于前面的交易额）；上述2个操作可以试着调整先后顺序看结果是否一致`<br><br>**预期结果**`钱包B向钱包A发送代币的交易成功，钱包A的交易可能成功也可能失败，不会出现其他异常情况`|
|INTR_Send_000140|同时发送、接收代币测试（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000131|同时向不同节点发送、接收代币测试（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的代币`<br><br>**输入内容**`在生成区块的间隔，钱包A通过节点X向另外一个钱包发送代币（交易额大于余额），同时钱包B通过节点Y向钱包A发送代币（可使钱包A的余额大于前面的交易额）；上述2个操作可以试着调整先后顺序看结果是否一致`<br><br>**预期结果**`钱包B向钱包A发送代币的交易成功，钱包A的交易可能成功也可能失败，不会出现其他异常情况`|
|INTR_Send_000141|同时向不同节点发送、接收代币测试（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000150|同时发送底层币、代币测试（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币、代币`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发送底层币交易，使钱包余额不足以支付下一次的燃料；同时该钱包发起一个发送代币交易；上述2个操作可以试着调整先后顺序看结果是否一致，也可以尽量同时发起这2个交易`<br><br>**预期结果**`2个交易中一个成功，一个失败，不会出现其他异常情况`|
|INTR_Send_000160|同时发送底层币、代币测试（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Send_000151|同时向不同节点发送底层币、代币测试（通过FCJT_sendTransaction测试）|**预置条件**`发送钱包有一定的底层币、代币`<br><br>**输入内容**`在生成区块的间隔，某钱包通过节点X发起一个发送底层币交易，使钱包余额不足以支付下一次的燃料；同时该钱包又通过节点Y发起一个发送代币交易；上述2个操作可以试着调整先后顺序看结果是否一致，也可以尽量同时发起这2个交易`<br><br>**预期结果**`2个交易中一个成功，一个失败，不会出现其他异常情况`|
|INTR_Send_000161|同时向不同节点发送底层币、代币测试（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|

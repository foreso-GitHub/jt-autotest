|用例编号|用例标题|预置条件<br>输入内容<br>预期结果|
|----------------|----------------|----------------|
|INTR_SendIssue_00001<br>0|同一钱包同时发送底层币和发行代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包有底层币`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发送底层币交易（使底层币不足以满足下一次交易的燃料），同时发起一个发行代币交易；可以尝试一下调整先后顺序`<br><br>**预期结果**`应该一个交易成功，另一个失败，不会出现其他异常情况`|
|INTR_SendIssue_00002<br>0|同一钱包同时发送底层币和发行代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_SendIssue_00003<br>0|不同钱包同时发送底层币和发行代币（通过FCJT_sendTransaction测试）|**预置条件**`发行代币的钱包燃料不够`<br><br>**输入内容**`在生成区块的间隔，钱包A发起发行代币的交易（但是燃料不够），钱包B向钱包A发送底层币使其燃料足够`<br><br>**预期结果**`发送底层币的交易成功，发行代币的交易可能失败，不会出现其他异常情况`|
|INTR_SendIssue_00004<br>0|不同钱包同时发送底层币和发行代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_SendIssue_00005<br>0|同一钱包同时发送底层币和发行、增发、销毁代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包有底层币`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发送底层币交易（使底层币余额不足以满足其他所有交易所需的燃料，只能满足部分），同时发起一个发币交易、一个增发交易、一个销毁交易`<br><br>**预期结果**`部分交易成功，部分失败，不会出现其他异常情况`|
|INTR_SendIssue_00006<br>0|同一钱包同时发送底层币和发行、增发、销毁代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_SendIssue_00007<br>0|同一钱包同时发送代币和发行代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发行代币交易，同时发起一个发送该代币的交易`<br><br>**预期结果**`发行代币交易应该成功，发送代币交易可能失败，不会出现其他异常情况`|
|INTR_SendIssue_00008<br>0|同一钱包同时发送代币和发行代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_SendIssue_00009<br>0|同一钱包同时发送代币和增发代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发送代币的交易，同时发起一个增发代币交易；发送代币交易额大于原来的余额，但是小于余额和增发数之和`<br><br>**预期结果**`增发代币成功，发送代币可能失败，不会出现其他异常情况`|
|INTR_SendIssue_00010<br>0|同一钱包同时发送代币和增发代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_SendIssue_00011<br>0|同一钱包同时发送代币和销毁代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发送代币的交易，同时发起一个销毁代币交易；发送代币交易额小于原来的余额，但是大于余额和销毁数之差`<br><br>**预期结果**`发送代币可能失败，销毁代币应该成功，不会出现其他异常情况`|
|INTR_SendIssue_00012<br>0|同一钱包同时发送代币和销毁代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_SendIssue_00013<br>0|同一钱包同时发送代币、增发代币、和销毁代币_01（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发送代币的交易，同时发起一个增发代币交易、一个销毁代币交易；发送代币交易额大于原来的余额，但是小于余额+增发数-销毁数`<br><br>**预期结果**`增发和销毁应该成功，发送代币可能失败，不会出现其他异常情况`|
|INTR_SendIssue_00014<br>0|同一钱包同时发送代币、增发代币、和销毁代币_01（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_SendIssue_00015<br>0|同一钱包同时发送代币、增发代币、和销毁代币_02（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发送代币的交易，同时发起一个增发代币交易、一个销毁代币交易；发送代币交易额小于原来的余额，但是大于余额+增发数-销毁数`<br><br>**预期结果**`增发和销毁应该成功，发送代币可能失败，不会出现其他异常情况`|
|INTR_SendIssue_00016<br>0|同一钱包同时发送代币、增发代币、和销毁代币_02（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|

|用例编号|用例标题|预置条件<br>输入内容<br>预期结果|
|----------------|----------------|----------------|
|INTR_Issue_000010|不同钱包同时发行symbol相同的代币（通过FCJT_sendTransaction测试）|**预置条件**`每个钱包都有足够的燃料`<br><br>**输入内容**`在生成区块的间隔，多个钱包同时发行代币，它们的symbol都一致，其他参数可以不一致`<br><br>**预期结果**`最多只有一个钱包的发币交易成功，其他都失败或者全部失败，不会出现其他异常情况`|
|INTR_Issue_000020|不同钱包同时发行symbol相同的代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000030|同一钱包同时发行symbol相同的代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个发行代币交易，它们的symbol都一致，其他参数可以不一致`<br><br>**预期结果**`最多只有一个发币交易成功，其他都失败或者全部失败，不会出现其他异常情况`|
|INTR_Issue_000040|同一钱包同时发行symbol相同的代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000050|同一钱包同时发行symbol不同的代币_燃料足够（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个发行代币交易，它们的symbol都不一致且有效，其他参数也都有效`<br><br>**预期结果**`所有发币交易成功`|
|INTR_Issue_000060|同一钱包同时发行symbol不同的代币_燃料足够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000070|同一钱包同时发行symbol不同的代币_燃料不够（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料不足`<br><br>**输入内容**`在生成区块的间隔，某钱包同时发起多个发行代币交易，它们的symbol都不一致且有效，其他参数也都有效，但是钱包燃料只够部分交易成功，不足以使所有交易都成功`<br><br>**预期结果**`部分发币交易成功，部分失败，不会出现其他异常情况`|
|INTR_Issue_000080|同一钱包同时发行symbol不同的代币_燃料不够（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000090|同一钱包同时发行和增发代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发行可增发代币的交易（参数都有效），同时发起一个增发该代币的交易`<br><br>**预期结果**`发币交易应该成功，增发交易应该失败，不会出现其他异常情况`|
|INTR_Issue_000100|同一钱包同时发行和增发代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000110|同一钱包同时发行和销毁代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发行可销毁代币的交易（参数都有效），同时发起一个销毁该代币的交易`<br><br>**预期结果**`发币交易应该成功，销毁交易应该失败，不会出现其他异常情况`|
|INTR_Issue_000120|同一钱包同时发行和销毁代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000130|同一钱包同时发行、增发、销毁代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个发行可增发可销毁代币的交易（参数都有效），同时发起一个增发代币的交易和销毁该代币的交易`<br><br>**预期结果**`发币交易应该成功，增发和销毁交易应该失败，不会出现其他异常情况`|
|INTR_Issue_000140|同一钱包同时发行、增发、销毁代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000150|同一钱包同时增发、销毁代币_足够销毁（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够并有可增发可销毁的代币`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个增发代币的交易，同时发起一个销毁该代币的交易；余额+增发数>销毁数`<br><br>**预期结果**`增发销毁都成功`|
|INTR_Issue_000160|同一钱包同时增发、销毁代币_足够销毁（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000170|同一钱包同时增发、销毁代币_不够销毁（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够并有可增发可销毁的代币`<br><br>**输入内容**`在生成区块的间隔，某钱包发起一个增发代币的交易，同时发起一个销毁该代币的交易；余额+增发数<销毁数`<br><br>**预期结果**`增发成功，销毁失败`|
|INTR_Issue_000180|同一钱包同时增发、销毁代币_不够销毁（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000190|同一钱包同时增发代币（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够并有可增发的代币`<br><br>**输入内容**`在生成区块的间隔，某钱包发起多个增发代币的交易`<br><br>**预期结果**`增发交易应该都成功`|
|INTR_Issue_000200|同一钱包同时增发代币（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000210|同一钱包同时销毁代币_足够销毁（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够并有可销毁的代币`<br><br>**输入内容**`在生成区块的间隔，某钱包发起多个销毁代币的交易，销毁总量小于代币余额`<br><br>**预期结果**`销毁交易应该都成功`|
|INTR_Issue_000220|同一钱包同时销毁代币_足够销毁（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
|INTR_Issue_000230|同一钱包同时销毁代币_不够销毁（通过FCJT_sendTransaction测试）|**预置条件**`钱包燃料足够并有可销毁的代币`<br><br>**输入内容**`在生成区块的间隔，某钱包发起多个销毁代币的交易，销毁总量大于代币余额`<br><br>**预期结果**`部分销毁交易成功，部分失败，不会出现其他异常情况`|
|INTR_Issue_000240|同一钱包同时销毁代币_不够销毁（通过FCJT_signTransaction测试）|**预置条件**`同上`<br><br>**输入内容**`同上`<br><br>**预期结果**`同上`|
